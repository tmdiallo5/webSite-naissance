
spring:
  application:
    name:
      ms-naissance
  jpa:
    hibernate:
      ddl-auto: validate
  jackson:
    default-property-inclusion: non_null
server:
  servlet:
    context-path: /api
rsa:
  rsa-private-key: classpath:certs/private.pem
  rsa-public-key: classpath:certs/public

